services:
  - type: web
    name: ai-voice-detector-backend
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn Backend_app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
