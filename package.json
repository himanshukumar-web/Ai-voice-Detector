{
    "name": "ai-voice-detector-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
          "build:ui": "cd ui && npm install && npm run build",
          "start:backend": "python -m uvicorn Backend_app.main:app --host 0.0.0.0 --port 8000"
    }
}
